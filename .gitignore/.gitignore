class Solution(object):

    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        self.nums = nums
        self.target = target
        self.L = []
        for n in range(len(self.nums)):
            x = self.target - self.nums[n]
            if x in self.nums:
                self.L.append(n)
                self.L.append(self.nums.index(x))
        if len(self.L) == 0:
            print('Have on answer')
        else:
            return list(set(self.L))

